using EventChannels;
using UnityEngine;

namespace ScriptableObjects.EventChannels.UI
{
    [CreateAssetMenu(menuName = "Event/UI/InputFieldUsed", fileName = nameof(InputFieldUsed))]
    public sealed class InputFieldUsed : EventChannel
    {
        
    }
}
